[project]
name = "futurefest2026-api"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "ekobelth.main:app"

[tool.vercel]
entrypoint = "ekobelth.main:app"
