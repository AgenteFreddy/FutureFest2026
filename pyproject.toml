[project]
name = "futurefest2026-api"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "ekobelth.main:app"

[tool.vercel]
entrypoint = "ekobelth.main:app"

echo '[project]' > pyproject.toml
echo 'name = "dummy"' >> pyproject.toml
echo 'version = "0.0.1"' >> pyproject.toml
